# C++项目编译与链接知识梳理

> 记录：使用源码编译的pinocchio库时遇到的问题与解决方案

## 目录
1. [遇到的问题](#遇到的问题)
2. [源码编译 vs 系统安装](#源码编译-vs-系统安装)
3. [CMake的两种链接方式](#cmake的两种链接方式)
4. [编译时 vs 运行时](#编译时-vs-运行时)
5. [环境变量冲突](#环境变量冲突)
6. [最佳实践](#最佳实践)

---

## 遇到的问题

### 问题1：Anaconda环境库冲突 🔴

**现象**：
```bash
cmake: /home/rvector/anaconda3/envs/OmniRobCtrl/lib/libstdc++.so.6: 
       version `GLIBCXX_3.4.30' not found (required by cmake)
```

**原因**：
- Anaconda环境被激活，其库路径被添加到 `LD_LIBRARY_PATH`
- 系统工具（cmake、make、bash等）尝试加载Anaconda环境中的库
- Anaconda的库版本较旧，不满足系统工具的需求

**解决方案**：
```bash
# 使用干净的环境运行cmake和make
env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin \
       HOME=/home/rvector \
       TERM=xterm \
       cmake ..
```

**教训**：
- Anaconda环境会影响系统级工具的运行
- 编译系统级C++项目时，最好在干净的环境中进行
- 或者在 `~/.bashrc` 中只在需要时才激活conda环境

---

### 问题2：find_package找不到pinocchio配置文件 🔧

**现象**：
```bash
CMake Error: Could not find a package configuration file provided by "pinocchio"
```

**原因**：
pinocchio虽然编译了，但CMake配置文件分散在不同位置：
- `pinocchioConfig.cmake` → `build/generated/`
- `pinocchioTargets.cmake` → `build/CMakeFiles/Export/lib/cmake/pinocchio/`
- `cxx-standard.cmake` → `cmake/`

这些文件互相引用，但路径不统一，导致 `find_package()` 失败。

**为什么会这样？**
因为pinocchio只是编译（build），没有安装（install）！

---

## 源码编译 vs 系统安装

这是理解整个问题的核心！

### 情况1：系统安装（标准方式）

```bash
cd pinocchio/build
cmake ..
make
sudo make install  # 👈 关键步骤！
```

**安装后的文件布局**：
```
/usr/local/
├── include/pinocchio/          # 头文件
├── lib/
│   ├── libpinocchio_default.so # 库文件
│   └── cmake/pinocchio/        # CMake配置文件
│       ├── pinocchioConfig.cmake
│       ├── pinocchioTargets.cmake
│       └── ...
└── share/pinocchio/            # 其他资源
```

**使用方式（简单）**：
```cmake
find_package(pinocchio REQUIRED)  # ✅ 能找到
target_link_libraries(main pinocchio::pinocchio)  # ✅ 自动处理
```

**优点**：
- ✅ CMake能自动找到
- ✅ 不需要手动指定路径
- ✅ 多个项目可以共享同一个库

**缺点**：
- ❌ 需要sudo权限
- ❌ 安装到系统目录，难以管理多个版本
- ❌ 修改源码后需要重新安装

---

### 情况2：源码编译（不安装，您的情况）

```bash
cd pinocchio/build
cmake ..
make
# 不执行 make install，库文件留在build目录
```

**文件布局（分散）**：
```
/home/rvector/pinocchio/
├── include/pinocchio/          # 头文件在源码目录
├── build/
│   ├── include/                # 生成的头文件
│   ├── src/
│   │   └── libpinocchio_default.so  # 库文件在build/src
│   ├── generated/
│   │   └── pinocchioConfig.cmake    # 配置文件（不完整）
│   └── CMakeFiles/Export/...        # 其他配置文件
└── cmake/                      # CMake模块
```

**使用方式（需要手动配置）**：

#### ❌ 方法1：尝试find_package（会失败）
```cmake
set(pinocchio_DIR /home/rvector/pinocchio/build/generated)
find_package(pinocchio REQUIRED)  # ❌ 会报错，文件路径不对
```

#### ✅ 方法2：直接指定路径（推荐）
```cmake
# 指定头文件路径
target_include_directories(main PUBLIC 
    /home/rvector/pinocchio/include        # 源码头文件
    /home/rvector/pinocchio/build/include  # 生成的头文件
)

# 直接链接库文件
target_link_libraries(main 
    /home/rvector/pinocchio/build/src/libpinocchio_default.so
)
```

**优点**：
- ✅ 不需要sudo权限
- ✅ 可以同时维护多个版本
- ✅ 修改源码后只需重新编译，不需要安装
- ✅ 库文件在源码目录，便于调试和管理

**缺点**：
- ❌ 需要手动配置路径
- ❌ 需要设置运行时库路径（LD_LIBRARY_PATH）
- ❌ 每个使用的项目都要配置一次

---

## CMake的两种链接方式

### 方式1：find_package + target（高级方式）

```cmake
find_package(pinocchio REQUIRED)
target_link_libraries(main pinocchio::pinocchio)
```

**工作原理**：
1. `find_package` 查找 `pinocchioConfig.cmake`
2. 该配置文件定义了 `pinocchio::pinocchio` 这个**导入目标（imported target）**
3. 这个目标包含了所有信息：
   - 头文件路径
   - 库文件路径
   - 依赖项
   - 编译选项

**优点**：
- 自动处理传递依赖
- 自动添加头文件路径
- 代码简洁

**前提条件**：
- 库必须提供完整的CMake配置文件
- 配置文件必须在CMake能找到的位置

---

### 方式2：直接链接（底层方式）

```cmake
# 手动指定头文件
target_include_directories(main PUBLIC 
    /path/to/include
)

# 直接链接.so文件
target_link_libraries(main 
    /path/to/libfoo.so
)
```

**工作原理**：
1. 直接告诉CMake头文件在哪
2. 直接告诉CMake库文件在哪
3. 不依赖任何配置文件

**优点**：
- 完全掌控，不依赖配置文件
- 适合源码编译、自定义库路径的情况
- 调试时容易理解

**缺点**：
- 需要手动管理依赖
- 需要手动指定所有路径
- 代码较冗长

---

## 编译时 vs 运行时

这是另一个容易混淆的概念！

### 编译时链接（Link Time）

**发生时机**：执行 `make` 或 `g++` 时

**作用**：
- 告诉链接器（linker）库文件在哪里
- 将符号（函数名、类名等）关联到具体的库
- 生成可执行文件

**CMake配置**：
```cmake
target_link_libraries(main 
    /home/rvector/pinocchio/build/src/libpinocchio_default.so
)
```

**编译时只需要知道**：
- 库文件的路径
- 库的符号表（函数、类的声明）

**查看编译结果**：
```bash
ldd build/main
# 输出：
# libpinocchio_default.so => /home/rvector/pinocchio/build/src/libpinocchio_default.so
```

---

### 运行时链接（Run Time）

**发生时机**：执行程序时（`./main`）

**作用**：
- 操作系统加载共享库（.so文件）到内存
- 将程序中的符号引用指向库中的实际代码

**需要的信息**：
- 库文件的路径必须在系统能找到的位置

**系统查找.so文件的顺序**：
1. 可执行文件中硬编码的 `RPATH`（编译时设置）
2. 环境变量 `LD_LIBRARY_PATH`
3. `/etc/ld.so.conf` 配置的路径
4. 系统默认路径：`/lib`, `/usr/lib`, `/usr/local/lib`

---

### 为什么需要设置LD_LIBRARY_PATH？

因为我们的库不在系统默认路径！

**问题场景**：
```bash
./main
# 错误：error while loading shared libraries: 
#       libpinocchio_default.so: cannot open shared object file
```

**解决方案1：临时设置（每次运行）**
```bash
LD_LIBRARY_PATH=/home/rvector/pinocchio/build/src:$LD_LIBRARY_PATH ./main
```

**解决方案2：写入脚本（推荐）**
```bash
#!/bin/bash
export LD_LIBRARY_PATH=/home/rvector/pinocchio/build/src:$LD_LIBRARY_PATH
./build/main
```

**解决方案3：在CMake中设置RPATH（最佳）**
```cmake
# 将库路径硬编码到可执行文件中
set_target_properties(main PROPERTIES
    BUILD_RPATH "/home/rvector/pinocchio/build/src"
    INSTALL_RPATH "/home/rvector/pinocchio/build/src"
)
```

这样就不需要每次设置环境变量了！

---

### 完整对比表

| 阶段 | 发生时机 | 需要什么 | 如何配置 | 错误信息 |
|------|---------|---------|---------|----------|
| **编译时** | 运行make | 库文件路径<br>头文件路径 | `target_link_libraries`<br>`target_include_directories` | `undefined reference to ...` |
| **运行时** | 运行程序 | 库文件能被找到 | `LD_LIBRARY_PATH`<br>或 `RPATH` | `cannot open shared object file` |

---

## 环境变量冲突

### 常见的环境变量

| 变量名 | 作用 | 影响阶段 |
|--------|------|----------|
| `PATH` | 可执行文件搜索路径 | 运行命令时 |
| `LD_LIBRARY_PATH` | 共享库搜索路径 | 程序运行时 |
| `CMAKE_PREFIX_PATH` | CMake包搜索路径 | CMake配置时 |
| `PKG_CONFIG_PATH` | pkg-config搜索路径 | CMake/编译时 |
| `C_INCLUDE_PATH` | C头文件搜索路径 | 编译时 |
| `CPLUS_INCLUDE_PATH` | C++头文件搜索路径 | 编译时 |

### Anaconda环境的影响

当激活conda环境时，它会修改：
```bash
export PATH="$CONDA_PREFIX/bin:$PATH"
export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:$LD_LIBRARY_PATH"
export CMAKE_PREFIX_PATH="$CONDA_PREFIX:$CMAKE_PREFIX_PATH"
```

**问题**：
- 系统工具（cmake、gcc等）可能使用conda的旧库
- 版本不兼容导致各种奇怪错误

**解决方案**：

#### 方案1：临时使用干净环境
```bash
env -i PATH=/usr/bin:/bin HOME=$HOME cmake ..
```

#### 方案2：只在需要时激活conda
```bash
# ~/.bashrc 中不要自动激活
# 只在需要时手动激活：
conda activate myenv
```

#### 方案3：使用conda的编译工具
```bash
conda install cmake compilers
# 使用conda的整套工具链
```

---

## 最佳实践

### 1. 源码编译不安装的场景（推荐用于开发）

**适用情况**：
- 正在开发/调试的库
- 需要同时维护多个版本
- 没有sudo权限

**配置模板**：

**CMakeLists.txt**：
```cmake
cmake_minimum_required(VERSION 3.10)
project(my_project)

set(CMAKE_CXX_STANDARD 14)

# 设置第三方库路径
set(PINOCCHIO_ROOT /home/rvector/pinocchio)
set(PINOCCHIO_BUILD ${PINOCCHIO_ROOT}/build)

# 查找系统依赖
find_package(Eigen3 REQUIRED)

# 添加可执行文件
add_executable(main src/main.cpp)

# 包含头文件
target_include_directories(main PUBLIC 
    ${PINOCCHIO_ROOT}/include
    ${PINOCCHIO_BUILD}/include
    ${EIGEN3_INCLUDE_DIR}
)

# 链接库文件
target_link_libraries(main 
    ${PINOCCHIO_BUILD}/src/libpinocchio_default.so
)

# 设置运行时库路径（推荐！）
set_target_properties(main PROPERTIES
    BUILD_RPATH "${PINOCCHIO_BUILD}/src"
    INSTALL_RPATH "${PINOCCHIO_BUILD}/src"
)
```

**优点**：设置了RPATH后，运行时不需要手动设置LD_LIBRARY_PATH！

---

### 2. 系统安装的场景（推荐用于生产）

**适用情况**：
- 库版本稳定，不经常改动
- 多个项目使用同一个库
- 有sudo权限

**安装**：
```bash
cd pinocchio/build
cmake -DCMAKE_INSTALL_PREFIX=/usr/local ..
make
sudo make install
```

**CMakeLists.txt**：
```cmake
cmake_minimum_required(VERSION 3.10)
project(my_project)

find_package(pinocchio REQUIRED)

add_executable(main src/main.cpp)

target_link_libraries(main pinocchio::pinocchio)
```

**优点**：简洁，不需要手动配置路径！

---

### 3. 混合场景（灵活）

有些库安装到系统，有些库使用源码：

```cmake
# 系统库：使用find_package
find_package(Eigen3 REQUIRED)

# 源码库：手动配置
set(MY_LIB_ROOT /home/user/my_lib)
target_include_directories(main PUBLIC ${MY_LIB_ROOT}/include)
target_link_libraries(main ${MY_LIB_ROOT}/build/libmylib.so)
```

---

## 调试技巧

### 1. 查看可执行文件依赖的库
```bash
ldd build/main
```

### 2. 查看库文件的符号
```bash
nm -D /path/to/libfoo.so | grep function_name
```

### 3. 查看可执行文件的RPATH
```bash
readelf -d build/main | grep RPATH
```

### 4. 查看CMake查找包的过程
```bash
cmake .. --debug-find
```

### 5. 查看链接过程的详细信息
```bash
make VERBOSE=1
```

---

## 总结：关键要点

### ✅ 理解三个阶段
1. **CMake配置**：找到库的位置，生成Makefile
2. **编译链接**：将代码编译成可执行文件，记录需要的.so
3. **运行时**：系统加载.so文件到内存

### ✅ 源码编译不安装时要注意
- 库文件在 `build/` 目录下
- CMake配置文件可能不完整
- 需要手动指定路径
- 需要设置 RPATH 或 LD_LIBRARY_PATH

### ✅ 环境变量要小心
- Anaconda会影响系统工具
- 编译时使用干净环境
- 或者使用conda的完整工具链

### ✅ 推荐实践
- 开发阶段：源码编译 + RPATH
- 生产环境：系统安装 + find_package
- 调试工具：ldd, nm, readelf

---

## 参考资料

- [CMake官方文档](https://cmake.org/documentation/)
- [Linux动态链接库](https://tldp.org/HOWTO/Program-Library-HOWTO/shared-libraries.html)
- [RPATH vs RUNPATH](https://stackoverflow.com/questions/42597685/understanding-rpath-runpath)
- [Pinocchio官方文档](https://stack-of-tasks.github.io/pinocchio/)

---

**创建日期**：2026-02-12  
**项目**：reBot_control  
**作者**：开发记录

